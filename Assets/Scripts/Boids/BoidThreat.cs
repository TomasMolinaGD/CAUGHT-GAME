using UnityEngine;

/// <summary>
/// Identifies an object that flock agents should consider a threat.
/// It intentionally contains no hunter logic, keeping both systems decoupled.
/// </summary>
public sealed class BoidThreat : MonoBehaviour
{
}
